{
  "name": "@clubshell/shell-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright end-to-end tests for the ClubShell kiosk UI, run against the Vite dev server in mock mode (VITE_MOCK=1)",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.47.0",
    "@types/node": "22.5.0",
    "typescript": "5.5.4"
  }
}
